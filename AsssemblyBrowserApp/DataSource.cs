using System;
using AsssemblyBrowserLib;

namespace AsssemblyBrowserApp
{
    class DataSource
    {
        /*public AsssemblyBrowserLib LoadAssembly(string path)
        {
            AssemblyBrowser browser = new AssemblyBrowser();
            Assembly asm = Assembly.LoadFrom(path);
            return browser.BrowseAssembly(asm);
        }*/
    }
}
