<Window x:Class="AsssemblyBrowserApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AsssemblyBrowserApp"
        mc:Ignorable="d"
        Title="MainWindow" Height="490" Width="743">
    <Grid Margin="0,0,3,-29.333">
        <TreeView x:Name="treeView"  HorizontalAlignment="Left" Height="346" Margin="10,10,0,0" VerticalAlignment="Top" Width="713" >
            <TreeView.Resources>
                <!--<HierarchicalDataTemplate ItemsSource="{Binding Path=Nodes}">
                    <TextBlock Text="{Binding Name}" />
                </HierarchicalDataTemplate>-->
            </TreeView.Resources>
        </TreeView>
        <Button Content="Choose Assembly" HorizontalAlignment="Left" Margin="310,376,0,0" VerticalAlignment="Top" Width="131" Height="41" Command="{Binding GetAssemblyCommand}"/>
    </Grid>
</Window>
